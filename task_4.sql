SELECT
    book_id,
    INT,
    ,NOT NULL
    PRIMARY_KEY,
    utf8,
    EXTRA
FROM
    alx_book_store.book_id, title, author_id, price, publication_date
WHERE
    Books= DATABASE()
    AND Books = "Books";
